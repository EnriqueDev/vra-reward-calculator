import * as React from 'react'
import { CssBaseline } from '@mui/material'
import { AppBar } from 'components'
const App: React.FC = () => {
  return (
    <React.StrictMode>
      <CssBaseline />
      <AppBar />
    </React.StrictMode>
  )
}

export default App
